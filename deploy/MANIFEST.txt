mousabatarseh-root.zip — built 2026-09-26T22:39:20.677Z
Unzip into the domain's document root (public_html). Top-level entries:

404.html                    0.0 MB   new file; only used if you add the optional ErrorDocument line
_assets/                    0.4 MB   new folder — CSS, JavaScript and fonts
about/                      0.0 MB   new folder — /about/ returns 404 on the live domain today (checked 2026-09-26)
apple-touch-icon.png        0.0 MB   new or replaces
blog/                       0.5 MB   REPLACES /blog/index.html and adds /blog/<post>/ folders — download the whole existing blog folder first
favicon.ico                 0.0 MB   replaces the existing favicon if there is one
favicon.svg                 0.0 MB   new or replaces
index.html                  0.1 MB   REPLACES the current home page — download the existing index.html first (step 2 of docs/deploy.md)
llms.txt                    0.0 MB   new or replaces — check whether the root already has one
media/                     75.1 MB   new folder — captures and recordings
og/                         3.3 MB   new folder — social sharing images
reviews/                    1.3 MB   REPLACES /reviews/index.html and adds /reviews/<review>/ folders; the old /reviews/reviews/, /reviews/builds/, /reviews/method/, /reviews/img/ and /reviews/assets/ stay on the server — download the whole existing reviews folder first
sitemap-portfolio.xml       0.0 MB   new file; the existing sitemap.xml is untouched
work/                       0.6 MB   new folder — /work/ returns 404 on the live domain today (checked 2026-09-26)

Not included on purpose: .htaccess at the root, robots.txt, sitemap.xml.
Redirects for the old post and review URLs: deploy/redirects.htaccess (add by hand — docs/deploy.md step 4).
Add this line to the existing robots.txt:
Sitemap: https://mousabatarseh.com/sitemap-portfolio.xml
